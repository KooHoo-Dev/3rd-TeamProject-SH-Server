# 학생용 — 서버 접속과 배포

> 처음 한 번만 30분쯤 걸린다. 그다음부터는 **명령 두 줄**이면 끝난다.

---
> 서버 주소 : `34.50.63.204`
> 서버 주소는 **바뀔 수 있음.** 안 붙으면 디코로 DM 보내셈

## 전체 그림

```
내 PC                          서버 (인터넷)
 │                                │
 ├─ 코드 고치고  git push ────────▶ GitHub
 │                                    │
 ├─ Rider 터미널로 접속 ──────────▶ ./deploy.sh
 │                                    └─ 받아오기 → 빌드 → 재시작
 │
 └─ 유니티 ──── ws://주소:포트/room ───▶ 내 서버
```

**소스는 GitHub 를 거쳐 간다.** 파일을 직접 올리지 않는다.

---

# 1단계 — 열쇠 만들기 (한 번)

서버는 비밀번호 대신 **열쇠 파일**로 사람을 알아본다. 열쇠는 **두 장이 한 짝**이다.

| | |
|---|---|
| **비밀키** `id_ed25519` | **절대 남에게 주지 않는다.** 내 컴퓨터에만 둔다 |
| **공개키** `id_ed25519.pub` | 서버에 등록한다. 남이 봐도 괜찮다 |

## 쉬운 길 — 선생님이 준 파일을 두 번 누른다

| 내 컴퓨터 | 누를 파일 |
|---|---|
| **윈도우** | `key gen.bat` |
| **맥** | `key gen.command` |

더블클릭하면 이 한 번으로 끝난다.

```
  서버 접속 열쇠 만들기
  ==========================================

  이름을 적으세요 (그냥 엔터도 됩니다) : 진호

  열쇠를 만들었습니다.

  아래 한 줄을 선생님께 보내세요.
  클립보드에도 복사해 두었으니 그냥 붙여넣기 하면 됩니다.
  ------------------------------------------
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIEkRFvED1... 진호@DESKTOP-ABC
  ------------------------------------------
```

**이미 클립보드에 들어 있으니 선생님께 바로 붙여넣기만 하면 된다.**

- 이름을 적는 이유는 하나다. 선생님이 다섯 장을 받았을 때 **누구 것인지 알아야** 한다.
- **두 번 눌러도 안전하다.** 이미 열쇠가 있으면 새로 만들지 않고 있는 것을 다시 보여 준다.
  (새로 만들면 선생님이 등록해 둔 것이 못 쓰게 된다)

### 맥에서 안 열릴 때

맥은 **인터넷에서 받은 파일을 바로 실행하지 않는다.** 두 가지로 막힌다.

| 증상 | 뜻 |
|---|---|
| 두 번 눌러도 아무 일도 안 남 | 실행 권한이 없다 |
| `확인되지 않은 개발자` 경고 | 다운로드 표시가 붙어 있다 |

**둘 다 한 번에 넘어가는 방법이 있다.** 터미널을 열고 `bash` 를 친 뒤 **한 칸 띄우고**,
그 파일을 터미널 창으로 **끌어다 놓고** 엔터를 친다.

```bash
bash /Users/내이름/Downloads/key\ gen.command
```

권한 자체를 주고 싶으면 아래를 한 번 친다. 그다음부터는 더블클릭이 된다.

```bash
chmod +x "/Users/내이름/Downloads/key gen.command"
```
---

# 2단계 — Rider 에 연결하기 (한 번)

터미널을 따로 열지 않고 **Rider 안에서** 서버를 쓸 수 있다.

## 접속 정보 등록

`File > Settings > Tools > SSH Configurations` 에서 `+` 를 누른다.

| 칸 | 값 |
|---|---|
| Host | `34.50.63.204` |
| Port | `22` |
| User name | `team3` ← **내 계정** |
| Authentication type | `Key pair` |
| Private key file | 윈도우 `C:\Users\내이름\.ssh\id_ed25519`<br>맥 `/Users/내이름/.ssh/id_ed25519` |

**비밀키(`.pub` 없는 쪽)를 고른다.** 아래 `Test Connection` 을 눌러 초록불이 뜨면 성공이다.

> 메뉴가 안 보이면 **`Ctrl + Shift + A`** 를 누르고 `SSH` 를 검색한다.
> Rider 판마다 위치가 조금 다를 수 있다.

## 터미널 열기

`Tools > Start SSH Session…` → 방금 만든 설정을 고른다.

Rider 아래쪽 터미널 탭에 서버 셸이 열린다.

```
team3@room-server:~$
```

**여기까지 오면 준비 끝이다.**

---

# 3단계 — 배포하기 (매번)

## 코드를 밀어 넣는다

내 PC 에서 평소처럼 한다.

```bash
git push
```

## 서버에 반영한다

Rider 터미널(서버에 붙어 있는 쪽)에서:

```bash
./deploy.sh
```

이 한 줄이 **받아오기 → 빌드 → 재시작**을 다 한다. 끝나면 이렇게 나온다.

```
-- 소스 받기
-- 빌드
-- 재시작

[OK] 실행 중 - 포트 5003
```

**`[OK]` 가 나오면 서버가 인터넷에서 살아 있다.**

## 로그 보기

터미널 탭을 **하나 더 열어** 로그를 띄워 두면 좋다.

```bash
journalctl --user -u app -f
```

유니티로 붙으면 여기에 `들어옴` 이 실시간으로 찍힌다. `Ctrl + C` 로 빠져나온다.

> **탭 두 개를 띄워 두는 것을 권한다.**
> 한쪽에서 배포하고, 다른 쪽에서 로그가 흐르는 걸 보면 무슨 일이 일어나는지 다 보인다.

---

# 4단계 — 유니티에서 붙기

`RoomClient` 의 `Url` 을 내 서버 주소로 바꾼다.

```
ws://34.50.63.204:5003/room
```

**`http` 가 아니라 `ws`** 이고, **포트는 내 것**이다.

## 잘 되는지 먼저 갈라 보기

유니티를 켜기 전에 **브라우저로** 확인하면 원인 찾기가 쉽다.

```
http://34.50.63.204:5003/ping
```

| 결과 | 뜻 |
|---|---|
| `pong` 이 보인다 | **서버는 정상.** 유니티 쪽을 본다 |
| 아무것도 안 나온다 | 서버가 안 떴다. `./deploy.sh` 부터 |

---

# 지켜야 할 규칙 하나

**`app.Run()` 은 반드시 비워 둔다.**

```csharp
app.Run();                        // ✅
app.Run("http://0.0.0.0:5000");   // ❌
```

포트는 **서버가 정해서 넣어 준다.** 코드에 적으면 내 포트가 아닌 곳으로 가서 아무도 못 붙는다.

배포할 때 이렇게 나오면 이 문제다.

```
[X] 떴지만 배정된 포트 5003 를 잡지 못했습니다.
```

---

# 막혔을 때

## 배포하다 나오는 말

| 나온 말 | 할 일 |
|---|---|
| `[OK] 실행 중 - 포트 500N` | 정상. 유니티로 붙어 보면 된다 |
| `[X] 떴지만 배정된 포트를…` | `app.Run()` 을 비운다 |
| `[X] 뜨지 못했습니다` | **바로 아래 로그**를 읽는다. 예외가 찍혀 있다 |
| `error CS…` | 컴파일 오류. 고쳐서 다시 push |
| `(앞에 빌드가 도는 중입니다…)` | 정상. 다른 팀이 빌드 중이라 기다린다 |
| `[X] ~/src 에 리포가 없습니다` | 선생님께 알린다 |

> **컴파일 오류가 나도 서버는 안 죽는다.** 직전에 성공한 버전이 계속 돈다.

## 접속이 안 될 때

| 증상 | 원인 |
|---|---|
| `Permission denied (publickey)` | 공개키가 아직 등록 안 됐다. 선생님께 확인 |
| `Connection timed out` | 서버 주소가 바뀌었거나 서버가 꺼져 있다 |
| Rider 에서 `Test Connection` 실패 | 비밀키 경로 확인. **`.pub` 가 아닌 쪽**이어야 한다 |
| 명령이 먹통 | `exit` 후 다시 접속 |

## 유니티가 못 붙을 때

**위에서부터 하나씩** 확인한다.

```
① 브라우저로 http://주소:내포트/ping — pong 이 보이나
       └ 안 보이면 → ./deploy.sh 부터 다시

② 유니티 Url 이 ws:// 로 시작하나 (http 아님)

③ 포트가 내 것인가 (남의 포트로 가면 남의 서버다)

④ 서버 로그에 "들어옴" 이 찍히나
       └ 찍히면 붙은 것이다. 그다음 문제는 주고받는 글자 쪽
```

---

# 요약 — 매일 하는 것

```bash
# 내 PC
git push

# Rider 터미널 (서버)
./deploy.sh
```

**이 두 줄이 전부다.** 나머지는 처음 한 번만 하면 된다.
